import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  textBox: {
    color: '#fff',
    backgroundColor: 'red',
    fontSize: 30,
    height: 100,
    textAlign: 'center',
    textAlignVertical: 'center',
    marginBottom: 10,
  },
});
